IronPython.StdLib
=================

IronPython stdlib .py files compiled to IronPython.StdLib.dll by pyc.py and script

Requires bash and mono. Tested on Fedora 19 with a custom build of mono from source

Download binaries zipfile from IronPython codeplex site
extract to a directory
copy build-stdlib.sh into the extracted directory
run from that directory
creates IronPython.StdLib.dll in that directory.

Currently version 2.7.4
